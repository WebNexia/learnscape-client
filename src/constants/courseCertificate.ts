/**
 * Course completion certificate PDF download for learners.
 * Flip to `true` when product is ready to ship certificates again (FE + BE share this intent).
 */
export const COURSE_CERTIFICATE_DOWNLOAD_ENABLED = false;
