export interface User {
	_id: string;
	username: string;
	email: string;
	password: string;
	role: string;
	orgId: string;
	imageUrl: string;
	isActive: boolean;
	createdAt: string;
	updatedAt: string;
}
